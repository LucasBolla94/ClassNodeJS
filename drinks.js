const nameUser = "Lucas";
const age = 2022 - 2005;
const minage = 21;

console.log(age);

if (age >= minage) {
    console.log(`${nameUser} you can drink alchool !`)
} else {
    console.log(`${nameUser} you can't drink alchool !`)
}